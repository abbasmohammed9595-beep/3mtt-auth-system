# 3MTT Software Development Step 22
Full Authentication System with Node.js, Express, bcrypt, JWT.

## How to run
1. npm install 
2. npm start

## Endpoints
POST /register - Create user with bcrypt password
POST /login - Return JWT access token
